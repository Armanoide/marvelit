import {DefaultTheme} from '@react-navigation/native';

export const NavigationTheme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: 'rgb(255, 45, 85)',
    navPrimaryColor: '#FFF',
  },
};
